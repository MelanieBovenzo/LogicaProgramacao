using System.Runtime.InteropServices;
using UnityEditor;
using UnityEngine;

public class EW7 : MonoBehaviour
{
    void Start()
    {
        int moedas = 0;

        while (moedas < 10)
        {
            if (moedas == 5)
            {
                print("Metade do caminho!");
            }
            i++;
        }
    }

    // Update is called once per frame
    void Update()
    {

    }
}
