using System.Runtime.InteropServices;
using UnityEditor;
using UnityEngine;

public class EW2 : MonoBehaviour
{
    void Start()
    {
        int i = 10;

        while (i >= 0)
        {
            print(i);
            i--;
        }
    }

    // Update is called once per frame
    void Update()
    {

    }
}
