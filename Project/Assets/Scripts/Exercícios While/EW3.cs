using System.Runtime.InteropServices;
using UnityEditor;
using UnityEngine;

public class EW3 : MonoBehaviour
{
    void Start()
    {
        int energia = 5;

        while (i >= 0)
        {
            print(energia);
            i--;
        }
    }

    // Update is called once per frame
    void Update()
    {

    }
}
